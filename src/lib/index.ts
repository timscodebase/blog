// This file is used to export all components in the lib folder. It makes is easier to import them in other files as `$lib/{component}`.

export { default as Header } from '$lib/header/header.svelte'
export { default as Footer } from '$lib/footer/footer.svelte'
